# 基于大语言模型的文档理解与多源数据融合系统 — 架构设计与详细方案

---

## 一、系统概述

### 1.1 项目定位

本系统旨在解决企事业单位在日常办公中面临的**非结构化文档信息提取、整合与自动化填表**难题。系统以大语言模型（LLM）为智能核心，结合文档解析、向量检索、结构化数据存储等技术，构建一站式的文档智能处理平台。

### 1.2 核心能力

| 能力 | 描述 |
| :--- | :--- |
| **文档理解** | 支持 `.docx`、`.xlsx`、`.md`、`.txt` 等多种格式文档的解析与语义理解 |
| **信息提取** | 基于 LLM 从非结构化文本中自动提取实体、关键字段和用户指定内容 |
| **数据融合** | 多源文档数据统一入库，支持跨文档关联查询与语义检索 |
| **自动填表** | 根据用户提供的模板表格，从已入库数据中自动匹配并填写对应字段 |
| **自然语言交互** | 用户通过自然语言指令完成文档操作、信息查询和数据管理 |

### 1.3 设计原则

- **模块化**：各功能模块松耦合，可独立开发、测试和部署
- **可扩展**：预留插件式架构，方便后续扩展新文档格式或新 AI 能力
- **高性能**：单文档处理响应时间 ≤90 秒，信息提取准确率 ≥80%
- **易用性**：提供直观的 Web UI，用户零学习成本即可上手操作

---

## 二、技术选型

### 2.1 技术栈总览

```
┌─────────────────────────────────────────────────────────┐
│                      前端 (Frontend)                     │
│  框架: React 18 + TypeScript                             │
│  UI 库: Ant Design 5.x                                   │
│  构建: Vite 5.x                                          │
│  HTTP: Axios                                              │
│  状态管理: Zustand                                       │
├─────────────────────────────────────────────────────────┤
│                      后端 (Backend)                       │
│  语言: Python 3.11+                                      │
│  框架: FastAPI 0.110+                                     │
│  异步: Uvicorn + asyncio                                  │
│  ORM: 无 (直接使用 pymongo 驱动)                          │
│  任务队列: Celery 5.x + Redis                             │
├─────────────────────────────────────────────────────────┤
│                   AI / NLP 引擎层                         │
│  LLM: OpenAI GPT-4o API / 智谱 GLM-4 API (可切换)        │
│  Embedding: text-embedding-3-small / bge-large-zh-v1.5    │
│  向量数据库: Milvus Lite (或 ChromaDB)                    │
│  文档解析: python-docx, openpyxl, markdown-it-py          │
│  OCR (可选): PaddleOCR                                   │
│  Prompt 编排: LangChain 0.2+                              │
├─────────────────────────────────────────────────────────┤
│                   数据存储层                               │
│  文档数据库: MongoDB 7.x                                  │
│  缓存 / 消息队列: Redis 7.x                               │
│  文件存储: 本地文件系统 / MinIO (可选)                     │
└─────────────────────────────────────────────────────────┘
```

### 2.2 选型理由

#### 2.2.1 前端 — React + TypeScript + Ant Design

| 考量点 | 说明 |
| :--- | :--- |
| **React 18** | 组件化开发，生态丰富，适合构建交互密集的文档操作界面 |
| **TypeScript** | 静态类型检查，减少运行时错误，提升团队协作效率 |
| **Ant Design 5** | 企业级 UI 组件库，内置表格、上传、表单等高质量组件，开箱即用 |
| **Vite** | 极快的冷启动和热更新，开发体验优异 |
| **Zustand** | 轻量级状态管理，API 简洁，适合中等规模应用 |

#### 2.2.2 后端 — Python + FastAPI

| 考量点 | 说明 |
| :--- | :--- |
| **Python** | AI/NLP 领域生态最成熟，LangChain、文档解析库均为 Python 原生 |
| **FastAPI** | 原生支持异步，自动生成 OpenAPI 文档，性能优于 Flask/Django |
| **Celery + Redis** | 成熟的异步任务方案，用于后台文档解析和 LLM 调用等耗时操作 |

#### 2.2.3 AI 引擎 — LLM + RAG

| 考量点 | 说明 |
| :--- | :--- |
| **GPT-4o / GLM-4** | 顶级语言理解能力，支持超长上下文，适合文档级别的结构化信息提取 |
| **RAG 架构** | 检索增强生成，先检索相关文档片段再交由 LLM 回答，提升准确性和可追溯性 |
| **LangChain** | 提供 Prompt Template、Chain、Agent 等抽象，简化多步骤 AI 编排 |
| **向量数据库** | 存储文档 Embedding、支持语义相似度搜索，是 RAG 的核心组件 |

#### 2.2.4 数据库 — MongoDB

| 考量点 | 说明 |
| :--- | :--- |
| **Schema-less** | 非结构化 → 半结构化数据的天然存储方案，无需预定义表结构 |
| **嵌套文档** | 可直接存储复杂的嵌套提取结果（如表格中的行列数据） |
| **全文索引** | 内置全文搜索，支持关键词级别的文档检索 |
| **水平扩展** | 支持分片和副本集，适应数据增长 |

---

## 三、系统总体架构

### 3.1 架构图

```
                          ┌──────────────┐
                          │   用户浏览器   │
                          └──────┬───────┘
                                 │ HTTPS
                          ┌──────▼───────┐
                          │  Nginx 反向   │
                          │   代理服务器   │
                          └──────┬───────┘
                     ┌───────────┼───────────┐
                     │                       │
              ┌──────▼───────┐       ┌───────▼──────┐
              │   前端静态    │       │   后端 API    │
              │  React App   │       │   FastAPI     │
              └──────────────┘       └───────┬──────┘
                                     ┌───────┼───────────────┐
                                     │       │               │
                              ┌──────▼──┐ ┌──▼──────┐ ┌──────▼──────┐
                              │ Celery  │ │ MongoDB │ │   Redis     │
                              │ Workers │ │  7.x    │ │  (缓存/MQ)  │
                              └────┬────┘ └─────────┘ └─────────────┘
                                   │
                         ┌─────────┼─────────┐
                         │                   │
                  ┌──────▼──────┐   ┌────────▼───────┐
                  │  LLM API    │   │  向量数据库      │
                  │ GPT-4o/GLM  │   │  Milvus/Chroma  │
                  └─────────────┘   └────────────────┘
```

### 3.2 分层说明

| 层级 | 职责 | 关键组件 |
| :--- | :--- | :--- |
| **展现层** | 用户交互、文件上传/下载、操作结果展示 | React, Ant Design |
| **网关层** | 静态资源服务、反向代理、HTTPS 终止 | Nginx |
| **应用层** | 业务逻辑编排、API 路由、认证鉴权 | FastAPI |
| **任务层** | 异步耗时任务执行（文档解析、LLM 调用） | Celery Workers |
| **AI 引擎层** | 文档理解、信息提取、语义检索、自动填表 | LangChain, LLM API |
| **数据层** | 结构化/半结构化数据持久化、缓存、向量存储 | MongoDB, Redis, Milvus |

---

## 四、模块详细设计

### 4.1 模块一：文档智能操作交互模块

#### 4.1.1 功能概述

基于自然语言处理与文档结构理解技术，用户通过**自然语言指令**即可完成对文档的编辑、排版、格式调整、内容提取等操作，无需手动操作文件。

#### 4.1.2 功能清单

| 功能 | 描述 |
| :--- | :--- |
| 自然语言解析 | 将用户输入的自然语言指令解析为结构化操作意图（Intent + Slot） |
| 文档预览 | 上传文档后在线预览原文内容，支持高亮、标注 |
| 智能编辑 | 通过对话指令实现内容修改、段落调整、格式设置等 |
| 内容摘要 | 对文档生成摘要、关键词和结构化大纲 |
| 操作历史 | 记录全部操作日志，支持撤销和回溯 |

#### 4.1.3 技术实现

```
用户指令 → Intent Recognition (LLM) → Action Router → Document Operator → 结果返回
```

**核心流程：**

1. **意图识别**：使用 LLM 对用户自然语言指令进行意图识别和槽位提取
   - Prompt 模板分类：`文档编辑`、`格式调整`、`内容提取`、`摘要生成`、`通用问答`
   - 输出标准化 JSON：`{"intent": "extract_table", "params": {"page": 2, "format": "csv"}}`

2. **动作路由（Action Router）**：根据意图分发到对应的处理器
   ```python
   # 伪代码
   ACTION_MAP = {
       "extract_table": TableExtractor,
       "summarize": DocumentSummarizer,
       "edit_content": ContentEditor,
       "format_adjust": FormatAdjuster,
       "qa": DocumentQA,
   }
   ```

3. **文档操作器（Document Operator）**：
   - 基于 `python-docx` 操作 Word 文档（段落增删改、样式设置、表格操作）
   - 基于 `openpyxl` 操作 Excel 表格（单元格读写、公式、样式）
   - 基于 `markdown-it-py` 解析和操作 Markdown 文件

4. **LangChain Agent 编排**：
   - 使用 LangChain 的 `Tool` 和 `Agent` 抽象，将文档操作封装为可被 LLM 调用的工具
   - Agent 根据用户指令自主规划工具调用链，实现多步骤复杂操作

#### 4.1.4 关键类与接口

```python
# app/modules/interaction/schemas.py
class UserCommand(BaseModel):
    """用户指令输入"""
    session_id: str          # 会话 ID
    command: str             # 自然语言指令
    document_ids: list[str]  # 关联文档 ID 列表

class CommandResult(BaseModel):
    """操作结果返回"""
    status: str              # success / error
    message: str             # 结果描述
    data: dict | None        # 结构化结果数据
    preview_url: str | None  # 文档预览链接
```

```python
# app/modules/interaction/agent.py
class DocumentAgent:
    """文档智能操作 Agent"""
    
    def __init__(self, llm, tools: list[BaseTool]):
        self.agent = initialize_agent(
            tools=tools,
            llm=llm,
            agent=AgentType.STRUCTURED_CHAT_ZERO_SHOT_REACT_DESCRIPTION,
        )
    
    async def execute(self, command: UserCommand) -> CommandResult:
        """执行用户指令"""
        ...
```

#### 4.1.5 API 接口

| 方法 | 路径 | 描述 |
| :--- | :--- | :--- |
| `POST` | `/api/v1/interaction/command` | 发送自然语言指令 |
| `GET` | `/api/v1/interaction/history/{session_id}` | 获取操作历史 |
| `POST` | `/api/v1/interaction/undo` | 撤销上一步操作 |
| `GET` | `/api/v1/interaction/preview/{document_id}` | 获取文档预览 |

---

### 4.2 模块二：非结构化文档信息提取模块

#### 4.2.1 功能概述

自动识别导入的多种格式非结构化文档，利用 LLM 深度理解文档语义，提取关键信息、实体数据和用户指定内容，并结构化存储至 MongoDB 数据库。

#### 4.2.2 功能清单

| 功能 | 描述 |
| :--- | :--- |
| 多格式解析 | 支持 `.docx`、`.xlsx`、`.md`、`.txt` 四种格式的自动识别与解析 |
| 文档分块 | 将长文档按语义分块（Chunking），保留上下文关联 |
| 向量化索引 | 对文档块进行 Embedding 并存入向量数据库，支持语义检索 |
| 实体提取 | 利用 LLM 提取人名、机构、日期、金额、地址等结构化实体 |
| 自定义提取 | 用户通过 Prompt 指定提取字段（如"提取所有项目名称与负责人"） |
| 数据入库 | 提取结果结构化后写入 MongoDB，支持查询和关联 |
| 批量处理 | 支持多文档批量上传和异步处理，任务队列管理 |

#### 4.2.3 处理流水线

```
文档上传 → 格式检测 → 文本提取 → 文档分块 → Embedding → 向量入库
                                      ↓
                              LLM 信息提取 → 结构化 → MongoDB 入库
```

**流水线各阶段详解：**

##### Stage 1: 文档解析（Document Parsing）

```python
# app/modules/extraction/parsers/base.py
class BaseParser(ABC):
    @abstractmethod
    def parse(self, file_path: str) -> ParsedDocument:
        """解析文档，返回统一的中间表示"""
        ...

# 具体解析器
class DocxParser(BaseParser):   # 使用 python-docx
class XlsxParser(BaseParser):  # 使用 openpyxl
class MarkdownParser(BaseParser):  # 使用 markdown-it-py
class TxtParser(BaseParser):   # 纯文本直接读取
```

**解析策略：**

| 格式 | 解析库 | 提取内容 |
| :--- | :--- | :--- |
| `.docx` | `python-docx` | 段落文本、表格数据、标题层级、图片描述 |
| `.xlsx` | `openpyxl` | 工作表名称、单元格数据、合并单元格信息、公式 |
| `.md` | `markdown-it-py` | 标题结构、段落、代码块、列表、表格 |
| `.txt` | 内置 `open()` | 纯文本内容，按段落分割 |

##### Stage 2: 文档分块（Chunking）

采用**语义感知分块策略**：

```python
# app/modules/extraction/chunker.py
class SemanticChunker:
    def __init__(
        self,
        chunk_size: int = 1000,       # 每块最大字符数
        chunk_overlap: int = 200,      # 块间重叠字符数
        separators: list[str] = ["\n\n", "\n", "。", ".", " "]
    ):
        self.splitter = RecursiveCharacterTextSplitter(
            chunk_size=chunk_size,
            chunk_overlap=chunk_overlap,
            separators=separators,
        )
    
    def chunk(self, document: ParsedDocument) -> list[DocumentChunk]:
        """将文档分割为语义连贯的块"""
        ...
```

**分块规则：**
- 优先按段落/章节级别分割
- 表格作为整体保持不拆分
- 设定 200 字符重叠区域保留上下文连贯性
- 每块附加元数据（来源文件名、页码/位置、标题层级）

##### Stage 3: 向量化与索引（Embedding & Indexing）

```python
# app/modules/extraction/embedder.py
class DocumentEmbedder:
    def __init__(self, model_name: str = "text-embedding-3-small"):
        self.embeddings = OpenAIEmbeddings(model=model_name)
        self.vector_store = Milvus(
            embedding_function=self.embeddings,
            collection_name="documents",
        )
    
    async def index(self, chunks: list[DocumentChunk]) -> None:
        """将文档块向量化并存入向量数据库"""
        texts = [c.content for c in chunks]
        metadatas = [c.metadata for c in chunks]
        await self.vector_store.aadd_texts(texts, metadatas)
    
    async def search(self, query: str, top_k: int = 5) -> list[DocumentChunk]:
        """语义检索最相关的文档块"""
        return await self.vector_store.asimilarity_search(query, k=top_k)
```

##### Stage 4: LLM 信息提取（Information Extraction）

```python
# app/modules/extraction/extractor.py
class InformationExtractor:
    EXTRACTION_PROMPT = """
    你是一个专业的信息提取助手。请从以下文本中提取结构化信息。
    
    提取要求：{requirements}
    
    文本内容：
    {text}
    
    请以 JSON 格式输出提取结果，确保所有字段准确无误。
    """
    
    async def extract(
        self,
        text: str,
        requirements: str,
        schema: dict | None = None
    ) -> dict:
        """
        从文本提取结构化信息
        - requirements: 用户指定的提取要求
        - schema: 可选的输出 JSON Schema，用于约束输出格式
        """
        ...
```

**提取策略：**
- **预定义提取**：系统内置常见实体类型（人名、机构、日期、金额、地址等）的提取 Prompt
- **自定义提取**：用户以自然语言描述需要提取的字段，LLM 动态构建提取方案
- **Schema 约束**：使用 JSON Schema 约束 LLM 输出格式，确保结构合规
- **多轮校验**：对提取结果进行格式校验，异常数据标记供人工复核

#### 4.2.4 数据模型（MongoDB）

```javascript
// 文档元数据集合: documents
{
  _id: ObjectId,
  filename: "合同文件.docx",
  file_type: "docx",
  file_size: 524288,
  upload_time: ISODate("2026-03-16T10:00:00Z"),
  status: "processed",  // pending | processing | processed | error
  chunk_count: 15,
  storage_path: "/data/uploads/xxxx.docx",
  created_by: "user_001"
}

// 文档块集合: document_chunks
{
  _id: ObjectId,
  document_id: ObjectId,           // 关联文档
  chunk_index: 0,                  // 块序号
  content: "第一章 项目概述...",    // 文本内容
  metadata: {
    source: "合同文件.docx",
    page: 1,
    heading: "第一章 项目概述",
    heading_level: 1
  }
}

// 提取结果集合: extraction_results
{
  _id: ObjectId,
  document_id: ObjectId,
  extraction_type: "entity",       // entity | custom | auto
  requirements: "提取所有甲乙方信息",
  results: {
    "甲方": "XX科技有限公司",
    "乙方": "YY信息技术集团",
    "合同金额": "500,000元",
    "签约日期": "2026-01-15",
    "项目名称": "智能办公系统建设"
  },
  confidence: 0.95,
  created_at: ISODate("2026-03-16T10:05:00Z")
}
```

#### 4.2.5 API 接口

| 方法 | 路径 | 描述 |
| :--- | :--- | :--- |
| `POST` | `/api/v1/documents/upload` | 上传文档（支持批量） |
| `GET` | `/api/v1/documents` | 获取文档列表 |
| `GET` | `/api/v1/documents/{id}` | 获取文档详情 |
| `DELETE` | `/api/v1/documents/{id}` | 删除文档 |
| `POST` | `/api/v1/extraction/extract` | 触发信息提取 |
| `GET` | `/api/v1/extraction/results/{document_id}` | 获取提取结果 |
| `POST` | `/api/v1/extraction/search` | 语义搜索 |
| `GET` | `/api/v1/tasks/{task_id}` | 查询异步任务状态 |

---

### 4.3 模块三：表格自定义数据填写模块

#### 4.3.1 功能概述

用户上传模板表格（Word 或 Excel 格式），系统自动分析表格结构和字段含义，从已入库的多源文档数据中检索匹配信息，自动完成表格填写，并生成可下载的填写结果文件。

#### 4.3.2 功能清单

| 功能 | 描述 |
| :--- | :--- |
| 模板分析 | 自动识别表格的结构、字段名称和填写要求 |
| 字段映射 | LLM 理解字段语义，建立模板字段与数据库中提取数据的映射关系 |
| 智能检索 | 基于 RAG 从向量数据库和 MongoDB 中检索与字段对应的数据 |
| 自动填写 | 将检索到的数据按正确格式填入模板表格的对应位置 |
| 结果校验 | 对填写结果进行完整性与格式校验，输出置信度评分 |
| 文件导出 | 生成填写完成的 Word/Excel 文件供用户下载 |

#### 4.3.3 核心流程

```
模板上传 → 表格结构解析 → 字段语义分析 → 数据检索 → LLM 填写决策 → 写入表格 → 校验 → 导出
```

**流程详解：**

##### Step 1: 模板结构解析

```python
# app/modules/filling/template_parser.py
class TemplateParser:
    """解析模板表格结构"""
    
    def parse_excel(self, file_path: str) -> TemplateSchema:
        wb = openpyxl.load_workbook(file_path)
        # 识别: 表头行、数据行区域、合并单元格、字段名、数据类型提示
        ...
    
    def parse_docx_table(self, file_path: str) -> TemplateSchema:
        doc = docx.Document(file_path)
        # 遍历 doc.tables，解析行列结构和字段
        ...

class TemplateSchema(BaseModel):
    """模板结构描述"""
    fields: list[FieldInfo]       # 字段列表
    structure: dict               # 表格布局信息
    file_type: str                # docx / xlsx

class FieldInfo(BaseModel):
    """字段描述"""
    name: str                     # 字段名称（如"项目名称"）
    position: tuple[int, int]     # 在表格中的位置 (row, col)
    data_type: str                # text / number / date / enum
    required: bool                # 是否必填
    description: str              # LLM 生成的语义描述
```

##### Step 2: 字段语义分析与数据检索

```python
# app/modules/filling/field_matcher.py
class FieldMatcher:
    """将模板字段与数据库中的提取数据进行语义匹配"""
    
    MATCHING_PROMPT = """
    你是一个数据匹配助手。以下是一个表格模板中需要填写的字段：
    {field_descriptions}
    
    以下是从多个文档中提取的数据：
    {extracted_data}
    
    请为每个字段找到最匹配的数据值，以 JSON 格式输出。
    如果某字段没有匹配的数据，值设为 null。
    确保数据类型匹配（日期用 YYYY-MM-DD 格式，金额保留两位小数等）。
    """
    
    async def match(
        self,
        template: TemplateSchema,
        document_ids: list[str]
    ) -> dict[str, Any]:
        # 1. 根据字段名称构建检索 query
        # 2. 从向量数据库检索相关文档块
        # 3. 从 MongoDB 检索已提取的结构化数据
        # 4. 将字段信息和检索结果交给 LLM 匹配
        ...
```

##### Step 3: 表格填写与导出

```python
# app/modules/filling/writer.py
class TableWriter:
    """将匹配结果写入模板表格"""
    
    def write_excel(self, template_path: str, data: dict, output_path: str):
        wb = openpyxl.load_workbook(template_path)
        ws = wb.active
        for field_name, value in data.items():
            row, col = self.field_positions[field_name]
            ws.cell(row=row, column=col, value=value)
        wb.save(output_path)
    
    def write_docx(self, template_path: str, data: dict, output_path: str):
        doc = docx.Document(template_path)
        for table in doc.tables:
            # 定位并填写对应单元格
            ...
        doc.save(output_path)
```

#### 4.3.4 API 接口

| 方法 | 路径 | 描述 |
| :--- | :--- | :--- |
| `POST` | `/api/v1/filling/upload-template` | 上传模板表格 |
| `POST` | `/api/v1/filling/execute` | 执行自动填表 |
| `GET` | `/api/v1/filling/result/{task_id}` | 获取填表结果 |
| `GET` | `/api/v1/filling/download/{task_id}` | 下载填写完成的文件 |
| `POST` | `/api/v1/filling/preview` | 预览填写结果（不保存） |

---

## 五、前端界面设计

### 5.1 页面结构

```
┌─────────────────────────────────────────────────────────────────┐
│  顶部导航栏: Logo | 文档管理 | 信息提取 | 智能填表 | 对话助手    │
├───────────────┬─────────────────────────────────────────────────┤
│               │                                                 │
│   左侧边栏    │                  主工作区                        │
│               │                                                 │
│  · 文档列表   │   根据当前功能模块展示不同内容：                  │
│  · 快速操作   │   - 文档管理: 文件列表 + 预览                    │
│  · 最近访问   │   - 信息提取: 提取面板 + 结果展示                │
│  · 任务队列   │   - 智能填表: 模板上传 + 填表进度 + 结果下载     │
│               │   - 对话助手: 聊天界面 + 操作记录                │
│               │                                                 │
├───────────────┴─────────────────────────────────────────────────┤
│  底部状态栏: 系统状态 | 任务进度 | 版本信息                      │
└─────────────────────────────────────────────────────────────────┘
```

### 5.2 核心页面

| 页面 | 主要组件 | 功能 |
| :--- | :--- | :--- |
| **文档管理** | 文件拖拽上传区、文档列表表格、文档预览抽屉 | 多文件批量上传、在线预览、删除管理 |
| **信息提取** | 文档选择器、提取配置面板、结果 JSON/表格视图 | 选择文档 → 配置提取需求 → 查看结构化结果 |
| **智能填表** | 模板上传区、字段映射预览、进度条、下载按钮 | 上传模板 → 预览映射 → 执行填表 → 下载结果 |
| **对话助手** | 聊天消息列表、输入框、操作建议卡片 | 自然语言交互，操作建议与执行 |

### 5.3 前端目录结构

```
src/
├── api/                    # API 请求封装
│   ├── documents.ts        # 文档管理 API
│   ├── extraction.ts       # 信息提取 API
│   ├── filling.ts          # 填表 API
│   └── interaction.ts      # 交互模块 API
├── components/             # 通用组件
│   ├── FileUploader/       # 文件上传组件
│   ├── DocumentPreview/    # 文档预览组件
│   ├── ChatPanel/          # 聊天面板
│   └── TaskProgress/       # 任务进度组件
├── pages/                  # 页面
│   ├── DocumentManage/     # 文档管理页
│   ├── Extraction/         # 信息提取页
│   ├── Filling/            # 智能填表页
│   └── ChatAssistant/      # 对话助手页
├── stores/                 # 状态管理 (Zustand)
│   ├── useDocumentStore.ts
│   └── useTaskStore.ts
├── utils/                  # 工具函数
├── App.tsx                 # 根组件
└── main.tsx                # 入口
```

---

## 六、后端项目结构

```
backend/
├── app/
│   ├── __init__.py
│   ├── main.py                     # FastAPI 应用入口
│   ├── config.py                   # 配置管理 (Pydantic Settings)
│   ├── dependencies.py             # 依赖注入
│   │
│   ├── api/                        # API 路由层
│   │   ├── __init__.py
│   │   ├── v1/
│   │   │   ├── documents.py        # 文档管理路由
│   │   │   ├── extraction.py       # 信息提取路由
│   │   │   ├── filling.py          # 填表路由
│   │   │   └── interaction.py      # 交互模块路由
│   │   └── router.py              # 路由注册
│   │
│   ├── modules/                    # 业务模块层
│   │   ├── interaction/            # 模块一: 文档智能操作交互
│   │   │   ├── agent.py            # LangChain Agent
│   │   │   ├── tools.py            # Agent 可用工具
│   │   │   ├── schemas.py          # 数据模型
│   │   │   └── service.py          # 业务逻辑
│   │   │
│   │   ├── extraction/             # 模块二: 非结构化文档信息提取
│   │   │   ├── parsers/            # 文档解析器
│   │   │   │   ├── base.py
│   │   │   │   ├── docx_parser.py
│   │   │   │   ├── xlsx_parser.py
│   │   │   │   ├── md_parser.py
│   │   │   │   └── txt_parser.py
│   │   │   ├── chunker.py          # 文档分块
│   │   │   ├── embedder.py         # 向量化
│   │   │   ├── extractor.py        # LLM 信息提取
│   │   │   ├── schemas.py
│   │   │   └── service.py
│   │   │
│   │   └── filling/                # 模块三: 表格自定义数据填写
│   │       ├── template_parser.py  # 模板解析
│   │       ├── field_matcher.py    # 字段匹配
│   │       ├── writer.py           # 表格写入
│   │       ├── schemas.py
│   │       └── service.py
│   │
│   ├── core/                       # 核心公共层
│   │   ├── llm.py                  # LLM 客户端封装
│   │   ├── database.py             # MongoDB 连接
│   │   ├── vector_store.py         # 向量数据库连接
│   │   ├── cache.py                # Redis 缓存
│   │   └── exceptions.py           # 全局异常定义
│   │
│   └── tasks/                      # Celery 异步任务
│       ├── __init__.py
│       ├── celery_app.py           # Celery 配置
│       ├── document_tasks.py       # 文档处理任务
│       └── filling_tasks.py        # 填表任务
│
├── tests/                          # 测试
│   ├── test_parsers/
│   ├── test_extraction/
│   └── test_filling/
│
├── requirements.txt                # Python 依赖
├── Dockerfile                      # Docker 构建
├── docker-compose.yml              # 容器编排
└── .env.example                    # 环境变量模板
```

---

## 七、核心技术方案

### 7.1 RAG（检索增强生成）架构

本系统的信息提取和自动填表能力均建立在 RAG 架构之上：

```
       ┌─────────────────────────────────┐
       │         用户查询 / 填表字段       │
       └──────────────┬──────────────────┘
                      │
              ┌───────▼───────┐
              │  Query Embedding │
              └───────┬───────┘
                      │
              ┌───────▼───────┐
              │  向量相似度检索  │ ←── Milvus / ChromaDB
              └───────┬───────┘
                      │ Top-K 相关块
              ┌───────▼───────┐
              │   上下文增强     │ ←── 补充 MongoDB 结构化数据
              └───────┬───────┘
                      │
              ┌───────▼───────┐
              │   LLM 生成回答  │ ←── GPT-4o / GLM-4
              └───────┬───────┘
                      │
              ┌───────▼───────┐
              │  结果后处理      │ ←── 格式校验、置信度评估
              └───────────────┘
```

### 7.2 Prompt 工程策略

系统设计了分层 Prompt 体系，确保 LLM 输出的稳定性和准确性：

| 层级 | 用途 | 示例 |
| :--- | :--- | :--- |
| **System Prompt** | 角色设定与全局约束 | "你是一个专业的文档信息提取助手..." |
| **Task Prompt** | 具体任务指令 | "请从以下合同文本中提取甲方、乙方..." |
| **Format Prompt** | 输出格式约束 | "请严格以 JSON 格式输出，字段包括..." |
| **Few-shot Examples** | 输入输出示例 | 提供 2-3 个标注样例供 LLM 参考 |

### 7.3 LLM 调用容错机制

```python
# app/core/llm.py
class LLMClient:
    """统一的 LLM 调用客户端，内置容错与重试"""
    
    def __init__(self, primary: str = "gpt-4o", fallback: str = "glm-4"):
        self.primary = primary
        self.fallback = fallback
    
    async def invoke(self, prompt: str, **kwargs) -> str:
        try:
            return await self._call(self.primary, prompt, **kwargs)
        except (RateLimitError, APIError):
            logger.warning("主模型调用失败，切换到备选模型")
            return await self._call(self.fallback, prompt, **kwargs)
    
    @retry(stop=stop_after_attempt(3), wait=wait_exponential(min=1, max=10))
    async def _call(self, model: str, prompt: str, **kwargs) -> str:
        """带指数退避重试的 API 调用"""
        ...
```

### 7.4 性能优化策略

| 策略 | 具体措施 | 预期效果 |
| :--- | :--- | :--- |
| **并行处理** | 多文档并行解析、多字段并行匹配 | 降低总体耗时 30%+ |
| **缓存复用** | Redis 缓存常用文档的 Embedding 和提取结果 | 避免重复 LLM 调用 |
| **流式输出** | LLM 流式返回 + SSE 推送前端 | 提升用户感知体验 |
| **分级处理** | 简单字段用规则匹配，复杂字段才调用 LLM | 减少 API 调用量 |
| **批量调用** | 将多个字段合并为一次 LLM 请求 | 减少网络延迟 |

---

## 八、部署架构

### 8.1 Docker Compose 一键部署

```yaml
# docker-compose.yml
version: "3.8"
services:
  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend

  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      - MONGODB_URL=mongodb://mongo:27017/docfusion
      - REDIS_URL=redis://redis:6379/0
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    depends_on:
      - mongo
      - redis

  celery-worker:
    build: ./backend
    command: celery -A app.tasks.celery_app worker -l info -c 4
    depends_on:
      - backend
      - redis

  mongo:
    image: mongo:7
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

volumes:
  mongo_data:
```

### 8.2 部署拓扑

```
                         ┌──────────────┐
                         │    Nginx     │ :80 / :443
                         └──────┬───────┘
                    ┌───────────┼───────────┐
                    │                       │
             ┌──────▼──────┐       ┌────────▼────────┐
             │  Frontend   │       │    Backend API   │ :8000
             │  (React)    │       │    (FastAPI)     │
             └─────────────┘       └────────┬────────┘
                                            │
                              ┌─────────────┼─────────────┐
                              │             │             │
                       ┌──────▼──┐   ┌──────▼──┐   ┌─────▼─────┐
                       │ MongoDB │   │  Redis  │   │  Celery   │
                       │  :27017 │   │  :6379  │   │  Workers  │
                       └─────────┘   └─────────┘   └───────────┘
```

---

## 九、关键依赖清单

### 9.1 后端 Python 依赖

```text
# Web 框架
fastapi==0.110.0
uvicorn[standard]==0.27.1
python-multipart==0.0.9

# 数据库
pymongo==4.6.2
motor==3.3.2              # MongoDB 异步驱动
redis==5.0.3

# AI / NLP
langchain==0.2.0
langchain-openai==0.1.3
langchain-community==0.2.0
openai==1.14.0
tiktoken==0.6.0

# 向量数据库
pymilvus==2.4.0           # 或 chromadb==0.4.24

# 文档解析
python-docx==1.1.0
openpyxl==3.1.2
markdown-it-py==3.0.0

# 异步任务
celery==5.3.6

# 工具
pydantic==2.6.4
pydantic-settings==2.2.1
tenacity==8.2.3           # 重试库
python-jose==3.3.0        # JWT (可选)
```

### 9.2 前端 NPM 依赖

```json
{
  "dependencies": {
    "react": "^18.3.0",
    "react-dom": "^18.3.0",
    "react-router-dom": "^6.22.0",
    "antd": "^5.15.0",
    "@ant-design/icons": "^5.3.0",
    "axios": "^1.6.7",
    "zustand": "^4.5.2",
    "dayjs": "^1.11.10"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.2.0",
    "@vitejs/plugin-react": "^4.2.0"
  }
}
```

---

## 十、安全与异常处理

### 10.1 安全措施

| 安全点 | 措施 |
| :--- | :--- |
| **文件上传** | 限制文件类型（白名单）、大小（50MB）、MIME 类型校验 |
| **API 认证** | JWT Token 认证（可选，演示阶段可关闭） |
| **SQL/NoSQL 注入** | 参数化查询，Pydantic 输入校验 |
| **LLM 安全** | Prompt 注入防护，输出内容过滤 |
| **CORS** | 配置允许的来源域名 |

### 10.2 异常处理策略

```python
# app/core/exceptions.py
class DocFusionError(Exception):
    """基础异常"""
    def __init__(self, message: str, code: int = 500):
        self.message = message
        self.code = code

class DocumentParseError(DocFusionError):
    """文档解析失败"""
    ...

class LLMInvocationError(DocFusionError):
    """LLM 调用失败"""
    ...

class ExtractionError(DocFusionError):
    """信息提取失败"""
    ...

# 全局异常处理器
@app.exception_handler(DocFusionError)
async def docfusion_error_handler(request, exc):
    return JSONResponse(
        status_code=exc.code,
        content={"error": exc.message}
    )
```

---

## 十一、创新要点

### 创新点一：多策略融合的智能信息提取引擎

结合 **规则匹配 + LLM 语义提取 + RAG 上下文增强** 三种策略，对于格式规范的数据（如日期、金额）优先使用正则规则精准提取，对于语义复杂的数据（如"项目负责人"、"合同条款"）使用 RAG + LLM 进行深度理解。既保证了提取速度，又确保了复杂场景下的准确率。

### 创新点二：基于 LLM Agent 的自适应表格填写

将表格填写任务建模为 **Agent 多步决策过程**，LLM Agent 自主规划字段填写策略：
1. 分析模板字段的语义含义
2. 构建检索 query 并从知识库中检索数据
3. 对检索结果进行推理、比对和筛选
4. 自主决定填写内容，并给出置信度评分

相比传统的关键词匹配方案，本方案在字段语义模糊、跨文档关联等场景下表现显著更优。

### 创新点三：对话式文档操作范式

打破传统"菜单 + 按钮"的文档操作方式，用户通过**自然语言对话**即可完成复杂的文档操作。系统将自然语言指令自动转化为可执行的文档操作序列，支持多步骤操作的链式执行和撤销回退。

---

## 十二、测试与验证方案

### 12.1 单元测试

| 模块 | 测试重点 |
| :--- | :--- |
| 文档解析器 | 各格式文件的正确解析，边界情况（空文件、超大文件、编码异常） |
| 分块器 | 分块大小、重叠区域、表格完整性 |
| 信息提取器 | 提取准确率、输出格式合规性 |
| 表格填写器 | 字段定位准确性、数据类型匹配、文件生成完整性 |

### 12.2 集成测试

- 端到端全流程测试：上传文档 → 解析 → 提取 → 上传模板 → 填表 → 下载
- 使用比赛方提供的样本集格式（5×docx + 3×md + 5×xlsx + 3×txt）进行模拟测试

### 12.3 性能测试

| 指标 | 目标 |
| :--- | :--- |
| 单文档提取响应时间 | ≤ 90 秒 |
| 信息提取准确率 | ≥ 80% |
| 批量文档上传（16 个文件） | ≤ 5 分钟完成全部解析 |
| 单次填表响应时间 | ≤ 90 秒 |

### 12.4 测试工具

- **后端**：`pytest` + `pytest-asyncio`
- **前端**：Vitest + React Testing Library
- **API 测试**：FastAPI TestClient / Postman
- **性能**：自定义计时脚本

---

## 附录

### A. 环境变量配置

```env
# .env
# LLM 配置
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxx
OPENAI_BASE_URL=https://api.openai.com/v1
LLM_MODEL=gpt-4o
EMBEDDING_MODEL=text-embedding-3-small

# 备选 LLM (智谱)
ZHIPU_API_KEY=xxxxxxxxxxxxxxxx
FALLBACK_LLM_MODEL=glm-4

# MongoDB
MONGODB_URL=mongodb://localhost:27017
MONGODB_DB_NAME=docfusion

# Redis
REDIS_URL=redis://localhost:6379/0

# Milvus
MILVUS_HOST=localhost
MILVUS_PORT=19530

# 文件存储
UPLOAD_DIR=/data/uploads
MAX_FILE_SIZE=52428800  # 50MB
```

### B. 技术栈版本矩阵

| 组件 | 版本 | 语言/运行时 |
| :--- | :--- | :--- |
| React | 18.3.x | TypeScript 5.4 |
| Vite | 5.2.x | Node.js 20 LTS |
| Ant Design | 5.15.x | — |
| FastAPI | 0.110.x | Python 3.11+ |
| LangChain | 0.2.x | Python |
| MongoDB | 7.x | — |
| Redis | 7.x | — |
| Milvus | 2.4.x | — |
| Celery | 5.3.x | Python |
| Docker | 24.x | — |
| Nginx | 1.25.x | — |
