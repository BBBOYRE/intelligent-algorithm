# 基于大语言模型与前后端分离的文档智能系统 — 开发文档

## 一、项目架构与演进

本项目已从早期的纯单体架构，全面**重构升级**为 **Vue 3 (前端) + FastAPI (后端) + 客户端脱机打包集成**的现代化大型分层工程架构体系。

### 1.1 核心技术栈演入
- **系统架构**：标准前后端分离 (RESTful API JSON 通信隔离)
- **底层 AI 引擎**：LangChain (链条构建) + Docling (精准版面结构切片) + ChromaDB (本地轻量化化内存向量库)
- **打包分发管道**：PyInstaller (`.spec` 钩子及隐藏树深度收集) + Inno Setup (Pascal 环境侦测脚本支持)

### 1.2 系统架构设计图

```mermaid
graph TB
    subgraph 客户端展示与外壳层
        VUE[Vue 3 SPA<br>静态编译应用前端]
        WEBVIEW[Windows WebView2<br>环境检测与承载]
    end

    subgraph 后端网关与路由
        FASTAPI[FastAPI + Uvicorn<br>RESTful endpoints 暴露点]
    end

    subgraph 核心应用处理层
        AGENT[LangChain Agent<br>对话意图解析与分发]
        PIPELINE[RAG 组装清洗管道<br>上传 / 入库 / 表格填写]
    end

    subgraph 数据支撑层 (外部化存储)
        DOCLING[Docling<br>复杂排版与表格结构智能切片]
        CHROMADB[(ChromaDB<br>本地 SQLite 强依赖型向量库)]
        EMBED[BGE 本地小模型<br>脱机环境下的 Embedding 引擎化]
    end

    VUE -->|Axios HTTP 请求| FASTAPI
    WEBVIEW -.承载.-> VUE
    FASTAPI --> AGENT
    FASTAPI --> PIPELINE
    PIPELINE --> DOCLING
    DOCLING --> EMBED
    EMBED --> CHROMADB
    AGENT --> CHROMADB
```

---

## 二、关键底层技术设计解密

### 2.1 Embedding 模型的脱机自本地化策略
在大型商用离线包体系中，由于终端网格限制环境原因，在 `core/knowledge_base.py` 中，特别敲定了基于路径存在性的自愈模型加载策略：
1. `config.py` 通过提取覆盖机制向外层 `.env` 开槽了参数 `EMBEDDING_MODEL=./data/models/bge-small-zh-v1.5`。
2. 当应用启动本地 `SentenceTransformer` 时，代码会主动按相对路径探测根服务 `data/models` 下的模型缓存指纹。
3. 若文件夹查无模型指纹，即判定为开发级冷启动，后台迅速调用 `huggingface_hub` 向官方仓库实行静默克隆拉取模型结构图及 `.safetensors` 文件。
4. 交付生产机打包时，数百 MB 的全尺寸模型库直接以外挂资源形式被平移处理（见下文 2.2），100% 豁免了最终客户翻出内网外下载模型环境的死锁尴尬。

### 2.2 PyInstaller 的 `_internal` 环境陷阱与外置资产脱壳剥离法
使用 PyInstaller (≥ 6.0) 进行 `--onedir` （单大文件夹）打包构建时，存在一个致命黑洞，它会强制剥离把所有源码、包环境强制打入一个名为 `_internal` 内部隔离文件夹。
- **业务痛点**：若在 `app.spec` 的 `datas` 元祖里直接装入用户私有数据参数集或模型文件，打包程序便会把它们顺理成章深层埋进隐藏的 `_internal` 结构深处，导致程序层以 Python 获取当前目录的相对路近时寻找模型文件以及 `.env` 发生绝对偏移，进而引发无环境支持的灾难性 HTTP 500 后台宕机。
- **根治解法**：在打包调度指挥部 `scripts/build.py` 的中控流程中，我们抛弃了强依赖 PyInstaller 托管关键读写静态数据的路径。而是在后置阶段，单写了一个 `Step 2.5` 构建钩子指令阶段。通过原生纯粹的 `shutil.copy` 系统能力去搬运，将项目根目录下的 `.env.example` 和 `data/models` **平滑地复制并暴露剥离提取到最终成品区 `dist/IntelligentDocSystem` 的系统环境的最外层**。这赋予了应用程序及最终端拥有对外部数据高度解耦与调优操作的能力。

### 2.3 基于 Inno Setup 安装包级别的自动静默补位运行库
针对分发给最终普通业务人员时，部分 Windows 框架环境由于残损极其容易导致系统页面白屏的问题，此前的做法显得苍白不堪，为此我们对 `scripts/installer.iss` 进行了深度工程防爆改进。我们在安装界面最外壳包装加入了独立内嵌的 Pascal 钩子注入脚本去抓取安装底层动态。
脚本执行探测如下：
- 指纹捕捉 `SOFTWARE\WOW6432Node\Microsoft\EdgeUpdate\Clients\...`
用以判读注册表中是否存在 WebView2 Edge 运行时基础支持。如果没有该底包，则弹框提示后马上在后台悄无声息调起系统静默网络下载模块，跨过一切从微软核心服务器处强拉资源并主动执行安装包运行进程。自此完美彻底告别纯前端构建出现的系统空窗白屏期。

---

## 三、部署及团队代码协作规范

### 3.1 关于源码版本防抖与编译强分层管理
出于编译阶段中海量 `build/` 环境与数十万层级的 `dist/` 文件可能会产生的污染反侵入问题：
- **`scripts/` 隔离执行法原则**：所有 Pyinstaller 驱动引擎的 `.spec` 和 `InnoSetup` 打包元脚本均已彻底驻留迁移至完全隔离独立的 `scripts` 高权文件夹内，并且利用工作 CWD（Current Working Directory）挂回项目根目录的操作原则，维持了全盘配置环境隔离与干净的沙盒体验。
- 此类分层让 `git status` 面板始终保持了原码与垃圾产物的彻底脱离及极端整洁的控制层级。
- 此外更利用了进阶的 **`.gitignore`** 正则规则策略排除了含有极大数据碎片的 `Installer/`、`data/models/` 及 `frontend/node_modules/` 对团队的代码提交阻断和强屏蔽。

### 3.2 增加新核心 API 路由（开发协定）
后续二次开发者续按现有网关约束标准加入新模块路由：
1. 请遵循逻辑下沉策略，于 `server/routes/...` 框架空间下分化单独的 Python 控制器路由器，并严丝合缝注册 `APIRouter`。
2. 切回顶端 `main.py` 入口向全局的 `app.include_router()` 追加挂载主线声明。
3. Vue 前端页面需平行在 `src/api` 层下补位 Axios HTTP 网络桥接存根函数来调取数据支撑。
